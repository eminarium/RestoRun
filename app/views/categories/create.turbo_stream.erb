<% if current_user.restaurant.categories.count > 1 %>
  <%= turbo_stream.prepend "#{current_user.id}-categories" do%>
    <%= render @category %>
  <% end %>
<% else %>
  <%= turbo_stream.update "#{current_user.id}-categories" do%>
    <%= render @category %>
  <% end %>
<% end %>